!!!
%html
  %head
    %title KISSR - Dropbox Web Hosting
    %meta{:name=>"description",:value=>"KISSr enables you to host websites out of your Dropbox"}
    %meta{:name=>"keywords",:value=>"kissr,hosting,web,web hosting,dropbox"}
    = stylesheet_link_tag 'application.css', :media => 'screen, projection'
    = stylesheet_link_tag 'print.css', :media => 'print'
    /[if IE]
      = stylesheet_link_tag 'ie.css', :media => 'screen, projection'

    = javascript_include_tag :defaults
    :javascript
      var uvOptions = {};
      (function() {
      var uv = document.createElement('script'); uv.type = 'text/javascript'; uv.async = true;
      uv.src = ('https:' == document.location.protocol ? 'https://' : 'http://') + 'widget.uservoice.com/t2KQD9b4S3imXDfVmH8ug.js';
      var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(uv, s);
      })();
      
    = csrf_meta_tag
  %body{:class => "#{controller_name}-#{action_name}"}
    .container
      %header
        -unless current_user.nil? then
          #welcome-message
            %p="Hi, #{current_user.name.split(' ')[0]}!"
            =link_to "Sign Out", signout_path, :class=> :button 
        %h5
          %a{:id=> "logo",:href=>"/"} Kissr
      .content
        #flash
          - flash.each do |key, value|
            %div{:id => "flash_#{key}"}= h value
        = yield
      %footer
        %nav
          %section
            %h3 Frequently Asked Questions
            %ul
              %li 
                %a{:href=>"http://docs.kissr.co/faq#dropbox"} What is Dropbox?
              %li 
                %a{:href=>"http://docs.kissr.co/faq#domains"} How do I get my own domain name?
          %section
            %h3 KISSr
            %ul
              %li 
                %a{:href=>"contact"} Contact
              %li 
                %a{:href=>"jobs"} Jobs
