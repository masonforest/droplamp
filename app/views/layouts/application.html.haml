!!!
%html
  %head
    = stylesheet_link_tag 'screen.css', :media => 'screen, projection'
    = stylesheet_link_tag 'print.css', :media => 'print'
    /[if IE]
      = stylesheet_link_tag 'ie.css', :media => 'screen, projection'

    = javascript_include_tag :defaults
    = csrf_meta_tag
  %body{:class => "#{controller_name}-#{action_name}"}
    .container
      %header
        -unless current_user.nil? then
          #welcome-message
            %p="Hi, #{current_user.name.split(' ')[0]}!"
            =link_to "Sign Out", signout_path, :class=> :button 
        %h5
          %a{:id=> "logo",:href=>"/"} Kissr
      .content
        #flash
          - flash.each do |key, value|
            %div{:id => "flash_#{key}"}= h value
        = yield
      %footer
        %nav
          %section
            %h3 Frequently Asked Questions
            %ul
              %li 
                %a{:href=>"docs/domains"} What is Dropbox?
              %li 
                %a{:href=>"docs/domains"} How does KISSr use Dropbox?
              %li 
                %a{:href=>"docs/domains"} How do I get my own domain name?
          %section
            %h3 KISSr
            %ul
              %li 
                %a{:href=>"contact"} Contact
              %li 
                %a{:href=>"jobs"} Jobs
